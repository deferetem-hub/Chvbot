import os
import telebot
from telebot import types

TOKEN = os.getenv("BOT_TOKEN")
bot = telebot.TeleBot(TOKEN)

ADMIN_ID = 5497155434  # <-- твой Telegram ID

# Товары
PRODUCTS = {
    "Встреча с родственником из прошлого": "Встреча с родственником из прошлого.

Хотите повстречаться со своим дедушкой, когда тот был совсем молодым? Или может быть с бабушкой, когда за ней ухлёстывали все её одногруппники? Легко!
Всё что вам нужно: иметь фотографии вашего родственника, желательно несколько хороших фото, чб или цветных, где видно лицо, может быть тело по пояс или по колено.
Очень желательно, чтобы старые фотографии были отсканированы, поскольку обычное фотографирование не передаёт всех мелочей. Если нет, то постараемся сделать что сможем с тем, как будут отфотографированы фото.

Помимо этого нужны фотографии других действующих лиц: вас, других родственников. Зависит от вашей фантазии.

Например, у вас есть фото вашей молодой мамы, допустим в колледже или вузе. И вы хотите сделать что-то совместное. Мы поместим вас рядом с мамой в том месте, где вам будет угодно: дома, в парке, на фоне Кремля. Маму можно будет обнять, поцеловать в щёку. Мы можем обсудить любой несложный сценарий.

Над каждой фотографией будет работать художник-ретушёр с пятилетним опытом работы в фотографии, поэтому готовый результат будет максимально индивидуальным и особенным.

Теперь к цифрам: время работы будет 3-5 дней. Стоимость работы до 25 февраля - 990 рублей за всё.

Кроме того, в уже готовое видео можно будет добавить 2 бесплатные правки. Все следующие правки +150 рублей каждая.

Если вы готовы оставить заявку и хотите, чтобы мы позвали менеджера - жмите "оставить заявку"",
    "Я с собой в детстве": "Описание товара 2",
    "Встреча с любимым персонажем": "Описание товара 3",
    "Превращение, морфинг": "Описание товара 4",
    "Свой вариант и стиль": "Описание товара 5",
}

# Храним выбранный товар пользователя
user_product = {}


# ---------- /start ----------
@bot.message_handler(commands=['start'])
def start(message):
    keyboard = types.ReplyKeyboardMarkup(resize_keyboard=True)
    keyboard.add("Старт")

    bot.send_message(
        message.chat.id,
        "Привет! Спасибо, что перешли в наш бот. Готовы начать?",
        reply_markup=keyboard
    )

    bot.send_message(
        ADMIN_ID,
        f"Пользователь @{message.from_user.username} нажал /start"
    )


# ---------- Поехали! ----------
@bot.message_handler(func=lambda m: m.text == "Поехали!")
def choose_service(message):
    keyboard = types.ReplyKeyboardMarkup(resize_keyboard=True)
    keyboard.add("Выбрать сценарий")

    bot.send_message(
        message.chat.id,
        "Отлично. Сейчас вам нужно выбрать сценарий для вашего видео.",
        reply_markup=keyboard
    )

    bot.send_message(
        ADMIN_ID,
        f"Пользователь @{message.from_user.username} нажал кнопку 'Поехали!'"
    )


# ---------- Выбрать сценарий ----------
@bot.message_handler(func=lambda m: m.text == "Выбрать сценарий")
def show_products(message):
    keyboard = types.ReplyKeyboardMarkup(resize_keyboard=True)
    for product in PRODUCTS:
        keyboard.add(product)

    bot.send_message(
        message.chat.id,
        "Супер. Вот те сценарии, которые чаще всего выбрают наши клиенты. Не стесняйтесь кликать и читать описания :) Как подберёте - нужно будет нажать "Оформить заявку". Вам напишут на ваш аккаунт в телеграм. Не забудьте проверить, чтобы вам могли писать неконтакты! (это можно сделать в настройках конфедициальности)",
        reply_markup=keyboard
    )

    bot.send_message(
        ADMIN_ID,
        f"Пользователь @{message.from_user.username} нажал кнопку 'Выбрать сценарий'"
    )


# ---------- Выбор товара ----------
@bot.message_handler(func=lambda m: m.text in PRODUCTS)
def product_info(message):
    user_product[message.chat.id] = message.text

    keyboard = types.ReplyKeyboardMarkup(resize_keyboard=True)
    keyboard.add("Назад", "Оформить заявку")

    bot.send_message(
        message.chat.id,
        PRODUCTS[message.text],
        reply_markup=keyboard
    )

    bot.send_message(
        ADMIN_ID,
        f"Пользователь @{message.from_user.username} выбрал '{message.text}'"
    )


# ---------- Назад ----------
@bot.message_handler(func=lambda m: m.text == "Назад")
def back_to_products(message):
    show_products(message)

    bot.send_message(
        ADMIN_ID,
        f"Пользователь @{message.from_user.username} нажал 'Назад'"
    )


# ---------- Оформить заявку ----------
@bot.message_handler(func=lambda m: m.text == "Оформить заявку")
def make_request(message):
    product = user_product.get(message.chat.id, "Неизвестно")

    bot.send_message(
        message.chat.id,
        "Спасибо! ✍️\nНапишите ваш вопрос, менеджер скоро ответит.",
        reply_markup=types.ReplyKeyboardRemove()
    )

    bot.send_message(
        ADMIN_ID,
        f"Пользователь @{message.from_user.username} нажал 'Оформить заявку' по товару '{product}'"
    )


# ---------- Любой текст после заявки ----------
@bot.message_handler(func=lambda m: True)
def forward_to_admin(message):
    bot.send_message(
        ADMIN_ID,
        f"💬 Сообщение от клиента\n"
        f"ID: {message.from_user.id}\n"
        f"Текст: {message.text}"
    )


print("Бот запущен...")
bot.infinity_polling()
